1.반복문
- 동일한 작업을 반복하는것임.

2.함수
- 한번에 한작업에 집중
- 읽기 쉽고 어떤 동작인지 알 수 있게 네이밍
- 자바스크립트는 순차적으로 실행 되면 인터프리터 언어라고 함
- 자바스크립트는 함수 실행 전 모든 함수들을 담고 있음(함수 선언식 방식)
- 호이스팅이란 스코프 내부 어디서든 변수 선언은 최상위에 선언된 것 처럼 행동 하는것임.
- 함수 표현식 경우 순차적으로 실행됨
- 화살표 함수 경우 일반 함수와 달리 자신만의 this를 가지지 않으며, 화살표 함수 내부에서 this를 사용하면, 그 this는 외부에서 값을 가져 옴.

3.배열
- 배열은 문자 외 숫자,객체,함수 등도 포함 할 수 있음
- shift는 배열 뒤에서 삭제 , unshift는 배열 앞에 값 추가

4.변수 추가적인 내용
- var 경우 한번 선언된 변수를 다시 선언 할 수 있다.
- var는 선언 하기전에 사용 할 수 있음.(호이스팅)
- var 는 함수 스코프(전역변수) 이고, let,const는 블록 스코프임(지역변수)

5. Temporal Dead Zone(TDZ)
- TDZ는 const, let, class 구문의 유효성에 영향을 미치는 중요한 개념이다. 
- TDZ는 선언 전에 변수를 사용하는 것을 허용하지 않는다.
- 반대로 var 변수는 선언 전에도 사용할 수 있기 때문에 var 사용은 피하는 것이 좋다.
- TDZ는 언어 스펙에 맞도록 좋은 코딩 습관을 만들어주기 때문에 좋다고 생각한다.

6. 스코프(Scope)
- 변수에 접근 할 수 있는 범위
- 전역 ,지역 스코프가 있음.

